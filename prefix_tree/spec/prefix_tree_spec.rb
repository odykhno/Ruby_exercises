require 'spec_helper'

describe PrefixTree do
  it 'has a version number' do
    expect(PrefixTree::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
